<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Entitlements for the hardened runtime.

  Electron needs the JIT and unsigned-memory allowances or V8 will not start
  under the hardened runtime. Network access is for the update check against
  the GitHub releases API. Nothing else is requested: BlinkBlink has no reason
  to touch the camera, microphone, contacts or the user's files.
-->
<plist version="1.0">
    <dict>
        <key>com.apple.security.cs.allow-jit</key>
        <true/>
        <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
        <true/>
        <key>com.apple.security.cs.disable-library-validation</key>
        <true/>
        <key>com.apple.security.network.client</key>
        <true/>
    </dict>
</plist>
